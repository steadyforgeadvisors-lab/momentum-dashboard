# Momentum Dashboard — No env vars required!
# The app works out of the box with localStorage.

# Optional: Add these only if you extend the app with Supabase cloud sync
# NEXT_PUBLIC_SUPABASE_URL=your-project-url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
